<%= form_with(model: stick) do |form| %>
  <% if stick.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stick.errors.count, "error") %> prohibited this stick from being saved:</h2>

      <ul>
        <% stick.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_area :email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
